# FastAPI and Core Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database
asyncpg==0.29.0

# Pydantic for data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Authentication
python-jose[cryptography]==3.3.0
python-multipart==0.0.6
passlib[bcrypt]==1.7.4
# passlib 1.7.4 detects the bcrypt backend via bcrypt.__about__.__version__,
# which bcrypt>=4.1.0 removed -- that breaks passlib's version handshake and
# manifests as "password cannot be longer than 72 bytes" on every hash/verify
# call, even for short passwords. Pin to the last 4.0.x release, which still
# has __about__ and is what passlib 1.7.4 was actually tested against.
bcrypt==4.0.1

# Background Jobs
celery==5.3.4
redis==5.0.1

# HTTP Client
httpx==0.24.1

# Environment Management
python-dotenv==1.0.0

# Date/Time Utilities
python-dateutil==2.8.2

# Image Processing
pillow==10.1.0

# Machine Learning
scikit-learn==1.3.2
numpy==1.24.3
pandas==2.1.4

# Development Dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-httpx==0.22.0
black==23.12.0
isort==5.13.2
mypy==1.8.0

# Monitoring
sentry-sdk[fastapi]==1.39.2

# Additional dependencies for user profile management
email-validator==2.1.0
bleach==6.1.0
aiofiles==23.2.1

# NYC Transit API
gtfs-realtime-bindings==1.0.0

# Web scraping pipeline (Playwright + DeepSeek LLM extraction)
playwright==1.40.0
openai==1.6.1

# Google Calendar integration (optional "Connect Google Calendar" feature)
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.149.0